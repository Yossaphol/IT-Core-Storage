tailwind.config = {
    theme: {
    extend: {
        fontFamily: {
        // กำหนดให้ 'Prompt' เป็นฟอนต์เริ่มต้นของ Tailwind (คลาส sans)
        sans: ['Prompt', 'sans-serif'],
        },
    },
    },
}